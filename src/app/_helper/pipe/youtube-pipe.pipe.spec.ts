import { YoutubePipePipe } from './youtube-pipe.pipe';

describe('YoutubePipePipe', () => {
  it('create an instance', () => {
    const pipe = new YoutubePipePipe();
    expect(pipe).toBeTruthy();
  });
});
